import { SearchJobDirective } from './search-job.directive';

describe('SearchJobDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchJobDirective();
    expect(directive).toBeTruthy();
  });
});
